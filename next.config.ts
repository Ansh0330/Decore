import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com','placehold.co'], // Add this line for Unsplash images
  },
};

export default nextConfig;
